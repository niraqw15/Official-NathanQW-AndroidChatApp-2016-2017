<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.livechatapp" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/live_chat_app_image"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="stateHidden" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChatActivity"
            android:label="@string/title_activity_chat"
            android:windowSoftInputMode="stateHidden"> <!--
            android:screenOrientation="portrait" NOTE: Forcing orientation is probably not the best way to fix the activity reset on rotation, but its the easiest quick fix -->
        </activity>
    </application>

</manifest>
